# Assign an integer (whole number) to a variable named 'age'
age = 25

# Assign a string (text) to a variable named 'name'
name = "Alice"

# Assign a float (decimal number) to a variable named 'pi'
pi = 3.14159

# Assign a boolean value (True or False) to a variable named 'is_adult'
is_adult = age >= 18  # Using an operator to check a condition

print(f"Hello, my name is {name} and I am {age} years old.")  # f-string for formatted output

if is_adult:
  print("You are an adult.")
else:
  print("You are not an adult yet.")
